%
% This code will make sure that the communication settings match those on the
% stack (baud rate and options).  
%

% make sure printer knows about setsccbatch and sccbatch!
statusdict /sccbatch known statusdict /setsccbatch known and not 
{ stop } if 

statusdict begin 25 sccbatch end
2 index eq
exch
3 index eq
and
{
   pop pop 
} {
   serverdict begin 0 exitserver
   statusdict begin 
   25 3 1 roll setsccbatch
} ifelse

