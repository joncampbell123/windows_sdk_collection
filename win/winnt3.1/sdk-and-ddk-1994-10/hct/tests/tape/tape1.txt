Uses the Windows NT Tape Backup applicaton in an automated
fashion to test the tape adapter and driver combination.

The following parameters are required :

Restore Drive  [default d:] (note w/o \)

In the default case, the test will backup the win nt system32
directory and restore that to drive d:, perform comparisons 
of the files and report the results as PASS or FAIL
