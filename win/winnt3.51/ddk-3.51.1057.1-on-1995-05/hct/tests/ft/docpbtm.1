@rem ************************************************************************
@rem
@rem Copyright (c) 1991 Microsoft Corporation
@rem
@rem Module Name:
@rem
@rem   docpbtm.1   
@rem
@rem Abstract:
@rem
@rem   This is the phase I script for the copy on boot mirror tests.  This
@rem   script formats the test drive, writes a file to it and reads that
@rem   file.  It then sets up for docpbtm.2 to run and establishes the
@rem   mirror.
@rem
@rem Author:
@rem
@rem   Kenieth Peery (t-kenpe) 12-9-92
@rem
@rem Revision History:
@rem
@rem *************************************************************************

call doinit

@rem
@rem Format the partition, write a file, read the file, etc...
@rem

call doformat %label% %fileSystem% %driveLetter1% %logfile% %FT_QFORMAT%

call dosetmap %driveLetter1%:\foo.bin %_fill_percent% %logfile%

call dodir    %driveLetter1% %logfile%   

call doendmd  %driveLetter1%:\foo.bin %logfile%   

call dochkdsk %driveLetter1% %fileSystem% %logfile%   

@rem
@rem Setup next script to run, establish the mirror and reboot
@rem

echo.                                      >> %logfile%
echo. TRACE: About to establish the mirror >> %logfile%
echo.                                      >> %logfile%

copy docpbtm.2 donext
mtrun estabmir.pcd /h
exit
