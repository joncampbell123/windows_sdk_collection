@rem ************************************************************************
@rem Copyright (c) 1991 Microsoft Corporation
@rem
@rem Module Name:
@rem   doextvol.2   
@rem
@rem Abstract:
@rem   This is the command script responsible for the extended volume set 
@rem   tests.
@rem   This script performs the following:
@rem     - calls doinit.cmd 
@rem     	- calls getdat.cmd to get the ft componet info  
@rem     	- calls scendat.cmd to get the name of the logfile
@rem     - performs a chkdsk on the extended volume set. 
@rem     - calls doendmd.cmd to perform and endtomd test on the original
@rem     - file created by fsetmap on the volume set
@rem     - calls dosetmap.cmd to run fsetmap on the extended ft component
@rem     - performs a dir tests
@rem     - copies the next do<testname>.* script to donext.
@rem     - runs the delete.mst script to remove the volume for the next stage of
@rem       the tests.  The system will reboot with donext copied to do.cmd.
@rem
@rem Author:
@rem   Mitch Millar  (a-mitchm) 4-13-94
@rem
@rem Revision History:
@rem *************************************************************************
@rem start phase 2

call doinit

call doendmd %driveLetter1%:\foox1.bin %logfile%

call dosetmap %driveLetter1%:\foox2.bin %_fill_percent% %logfile%  

call dodir %driveLetter1% %logfile%  

call doendmd %driveLetter1%:\foox2.bin %logfile%  

call dochkdsk %driveLetter1% %fileSystem% %logfile% 

copy do.0 donext
mtrun delete.pcd /h
exit
