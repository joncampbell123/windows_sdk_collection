 declare sub loging(s as string, logfile1 as string)
'*********************************************************************
'
'   Function:  loging()
'   Paramiters1      if "OPEN"  we will open log file
'                    if "CLOSE" we will close logfile
'                    any other string will be written to log
'   Paramiter2    logfile name
'   Returns      :void
'   Globals used : NON
'   Description  : A generic loging function. see paramiter list above on usage.
'*********************************************************************
sub loging(s as string, logfile1 as string)
    static outfile as integer
    static logfile as string
    if      UCASE$(s) = "OPEN"  then ' ---- Open log file
       outfile = freefile
        logfile = logfile1
        OPEN logfile for output as #outfile
    elseif  UCASE$(s) = "CLOSE" then ' ---- Close log file
        close #outfile
    else '----- write string to log
        print # outfile, s
    endif
end sub
