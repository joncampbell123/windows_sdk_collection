Unzip the archive with a product that support long filenames, like for example a recent version of WinZip32.
Place the custom effect (.so and .cus) under the directory pointed by the environment variable SI_CUSTOM_TOOLS.

